/* -- Footer */

footer { position: absolute; bottom: 10px; text-align: center; width: 100%;
  .social-icons {
    a { opacity: .5; color: #000; padding: 5px 10px; display: inline-block; font-size: 18px;
      &:hover { opacity: 1; }
    }
  }
}